services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: praveg
    ports:
      - "5432:5432"
    volumes:
      - praveg_db:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d praveg"]
      interval: 5s
      timeout: 3s
      retries: 12

volumes:
  praveg_db:
